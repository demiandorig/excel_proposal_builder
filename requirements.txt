fastapi>=0.110
uvicorn[standard]>=0.27
psycopg[binary]>=3.2
python-multipart>=0.0.9
openpyxl>=3.1
Pillow>=10.0
pydantic>=2.0
openai>=1.50   # needs the Responses API (client.responses) for Step 05's web_search_preview tool
python-docx>=1.1
python-pptx>=1.0   # the Net/Gross signature-ready PowerPoint export (Step 07)
python-dotenv>=1.0   # loads .env at startup (OPENAI_API_KEY, DRIVE_* creds, etc.)

# Optional — only needed if you want the /api/drive/upload endpoint to
# actually upload to Google Drive. Without these installed (or without the
# env vars set), Drive upload returns a graceful "not configured" stub.
google-api-python-client>=2.100
google-auth>=2.20
google-auth-httplib2>=0.2
google-auth-oauthlib>=1.0

# playwright: used by the UI smoke tests (smoke_test_ui*.py) AND by
# app/services/ad_presence.py (Meta/Google/TikTok ad-library live checks).
# Needs a one-time browser download after installing the package:
#   playwright install chromium
playwright
pytest

# ad_presence.py's en/es ad-copy language detection (Meta + TikTok samples)
langdetect>=1.0.9
